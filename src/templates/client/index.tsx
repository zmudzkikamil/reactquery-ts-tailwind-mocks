import { BrowserTracks } from '../../tracks/BrowserTracks';

export const TemplateClient: React.FC = () => {
	return (
		<main className=' bg-black-background text-white'>
			<BrowserTracks />
		</main>
	);
};
